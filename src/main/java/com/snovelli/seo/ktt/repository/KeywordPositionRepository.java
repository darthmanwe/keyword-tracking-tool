package com.snovelli.seo.ktt.repository;

import com.snovelli.seo.ktt.model.KeywordPosition;

import org.springframework.data.jpa.repository.JpaRepository;

public interface KeywordPositionRepository extends JpaRepository<KeywordPosition, Long> {
}
